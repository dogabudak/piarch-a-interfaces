// Use this where its needed
export enum Availability {
    featured,
    available
}
export enum AttractionType {
    Church,
    Column,
    Museum,
    Shop,
    Palace,
    Information,
    Mosque,
    Cistern,
    House,
    Square,
    Park,
    Castle,
}
export enum TourType {
    Bike,
    Hike,
    Trekking,
  }
