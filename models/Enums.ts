// Use this where its needed 
export enum Availability {
    featured,
    available
}
export enum AttractionType {
    Church,
    Mosque,
    Museum,
}
export enum TourType{
    Bike,
    Hike,
    Trekking,
  }